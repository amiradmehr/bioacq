# Build trees (normally kept outside the repo, see scripts/build.sh)
build/
build-*/
cmake-build-*/
out/
compile_commands.json

# Runtime output
recordings/

# Packaged builds (only committed on the mac / windows / linux branches)
dist/*
!dist/linux/

# OS / editor / tooling
.DS_Store
__pycache__/
*.pyc
.claude/
.vscode/

# device firmware backups (binary, device-specific)
emotibit-firmware-backup/
